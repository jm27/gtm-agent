# GTM Agent — Go-to-Market Orchestrator

# No API key needed for demo — runs in simulation mode
# Add one to use real LangGraph agents:
# GTMA_API_KEY=your-key-here
# OPENAI_API_KEY=your-key-here same as GTMA_API_KEY if using OpenAI LLM provider
# GTMA_LLM_PROVIDER=openai
